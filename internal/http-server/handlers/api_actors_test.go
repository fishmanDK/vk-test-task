package handlers

import "testing"

func TestHandler_getAllActors(t *testing.T) {
	cases := []struct {
		Name  string
		Male  string
		Birth string
		Films []string
	}{}
	for _, tc := range cases {
		_ = tc
	}
}
